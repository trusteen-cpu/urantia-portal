이 폴더에 다음 음성 파일이 필요합니다.

  01_reading.mp3  (낭독)  ← generate_audio_daily.py 실행 시 자동 생성
  02_sermon.mp3   (강론)  ← generate_audio_daily.py 실행 시 자동 생성
  03_song.mp3     (노래)  ← Mureka에서 만든 곡을 이 이름으로 넣어 주세요
  04_prayer.mp3   (기도)  ← generate_audio_daily.py 실행 시 자동 생성

노래(03_song.mp3)가 없어도 재생은 멈추지 않고 다음(기도)으로 넘어갑니다.
